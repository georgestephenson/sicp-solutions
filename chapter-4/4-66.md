# Exercise 4.66

As described in exercise 4.65, the problem is that the stream of values may contain duplicates if the same person is listed in multiple frames, and so their salary will be added to the total more than once.

In order to solve this, the procedure would need to deduplicate the result of streams, by checking for distinct results in the frame stream, and only extracting values and summing them from the distinct results.